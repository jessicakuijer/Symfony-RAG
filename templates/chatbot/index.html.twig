{% extends 'base.html.twig' %}

{% block title %}Hello RGESN-gpt !{% endblock %}

{% block body %}
    <h1>Hello RGESN-gpt !</h1>

    <article>
        {{ form(form) }}

        {{ answer|raw }}
    </article>
    {# <div>
        <pre>{{ answer }}</pre>
    </div> #}

{% endblock %}
